# --- Environment files (never commit these) ---
.env
.env.*
.env.local
.env.*.local
!.env.example

# --- Dependencies / build output ---
node_modules
dist
build

# --- Convex generated code (regenerated by `bun convex dev --once`) ---
# Uncomment the next line if you want to regenerate on every deploy:
# src/convex/_generated

# --- Logs & local tooling ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json